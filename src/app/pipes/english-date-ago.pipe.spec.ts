import { EnglishDateAgoPipe } from './english-date-ago.pipe';

describe('EnglishDateAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new EnglishDateAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
